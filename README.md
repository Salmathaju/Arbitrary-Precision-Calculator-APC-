# Arbitrary-Precision-Calculator-APC-
APC (Arbitrary Precision Calculator) is a command-line application developed in C to perform arithmetic operations on very large integers that exceed the limits of built-in data types.
